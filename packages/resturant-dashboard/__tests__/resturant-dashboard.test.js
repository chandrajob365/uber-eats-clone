'use strict';

const resturantDashboard = require('..');
const assert = require('assert').strict;

assert.strictEqual(resturantDashboard(), 'Hello from resturantDashboard');
console.info('resturantDashboard tests passed');
